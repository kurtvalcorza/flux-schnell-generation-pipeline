# Release status

Current status: **Candidate** — the `E2E` tutorial notebook `tutorials/flux_schnell_generation_colab.ipynb` is generated, standalone and statically validated, and the pipeline package with its QLoRA adaptation contract, the offline unit suite (41 tests on a stub transformer and temporary manifests), `MODEL_CARD.md` (MODEL_CARD_SPEC 1.1), the static validator (`tools/validate_release_assets.py`), the generator parity checks and the CI workflow exist and are green. No clean-runtime execution of the committed notebook blob has been recorded yet; promotion to **Release-grade** follows the exact committed blob executing top-to-bottom in a clean Kaggle Tesla T4 (or Colab) runtime with no repository checkout, recorded in `docs/release-verification.md`.

The DIMER upload of the weights is on **HOLD** by the maintainer's decision (2026-09-20), independently of the release gate. Two facts stand behind it, neither of them code: the upstream Hub repository `black-forest-labs/FLUX.1-schnell` is gated and hides its file digests, so the byte identity of the mirror-staged snapshot is proven by file sizes and consensus digests until the gate is accepted (`docs/WEIGHTS.md`); and the served set would be 33.7 GB, far above the fleet's 9 GB publication convenience gate.

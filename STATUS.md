# Release status

Current status: **Release-grade** — the `E2E` tutorial notebook `tutorials/flux_schnell_generation_colab.ipynb` (blob `52226812`, committed at `d4fdf72`) executed top-to-bottom in a clean Kaggle Tesla T4 runtime on 2026-09-20 (11/11 code cells ok (1 restart after the install cell), 2911.9 s, 130 files / 34,341 MB fetched and digest-verified inside the notebook (the 23-file FLUX snapshot from the mirror, the 9-file scorer, the 60 pinned photographs)), recorded in `docs/release-verification.md`; held-out flow-matching MSE (test) 0.795 → 0.507 and CLIP label accuracy 0.667 → 0.833 against the real-photo ceiling 0.917 (sample-sanity, not a benchmark); reload parity exact. Any later change to the carried modules or to the notebook yields a new blob and returns the registry to **Candidate** until a clean run of that blob is recorded. At that revision the pipeline package with its QLoRA adaptation contract, the offline unit suite (41 tests on a stub transformer and temporary manifests), `MODEL_CARD.md` (MODEL_CARD_SPEC 1.1), the static validator (`tools/validate_release_assets.py`), the generator parity checks and the CI workflow exist and are green.

The DIMER upload of the weights is on **HOLD** by the maintainer's decision (2026-09-20), independently of the release gate. Two facts stand behind it, neither of them code: the upstream Hub repository `black-forest-labs/FLUX.1-schnell` is gated and hides its file digests, so the byte identity of the mirror-staged snapshot is proven by file sizes and consensus digests until the gate is accepted (`docs/WEIGHTS.md`); and the served set would be 33.7 GB, far above the fleet's 9 GB publication convenience gate.
